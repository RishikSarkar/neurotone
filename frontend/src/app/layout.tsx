import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Neurotone | Early Detection of Dementia and Parkinson's",
  description: "Neurotone uses voice analysis to detect early-stage dementia and Parkinson's by capturing everyday speech patterns and flagging subtle changes linked to cognitive or motor decline.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>
        {children}
      </body>
    </html>
  );
}
